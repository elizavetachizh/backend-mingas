<h2 class="page-title">График приема</h2>
<a href="/admin/admin_table/add-table" class="btn btn-primary">Добавить новое</a>
<br><br>
<% if (count > 0) { %>
    <table class="table table-striped sorting">

        <thead>
        <tr class="home">
            <th>Информация</th>
            <th>Редактировать</th>
            <th>Удалить</th>
        </tr>
        </thead>
        <tbody>
        <% tableInfo.forEach(function(tabl){ %>
            <tr>
                <td><%= tabl.name %></td>
                <td><a href="/admin/admin_table/edit-table/<%= tabl._id %>">Редактировать</a></td>
                <td><a class="confirmDeletion" href="/admin/admin_table/delete-table/<%= tabl._id %>">Удалить</a></td>
            </tr>
        <% }); %>
        </tbody>
    </table>
<% } else {%>
    <h3 class="text-center">Нет ни одного созданного поста</h3>
<% }%>