<h2 class="page-title">Редактировать запись</h2>
<br><br>
<a href="/admin/admin_pressCenterVideo" class="btn btn-primary">Показать все записи</a>
<br><br>

<form method="post" action="/admin/admin_pressCenterVideo/edit-info/<%= id %>">

    <div class="form-group">
        <label for="name">Название</label>
        <input type="text" class="form-control" id="name" name="name" value="<%= name %>" placeholder="name">
    </div>

    <div class="form-group">
        <label for="link">Ссылка</label>
        <input type="text" class="form-control" id="link" name="link" value="<%= link %>" placeholder="link">
    </div>

    <button class="btn btn-default">Сохранить</button>

    <% if (errors) {%>
        <% errors.forEach(function(error) { %>
            <div class="alert alert-danger">
                <%= error.msg %>
            </div>
        <% });%>
    <% }%>
</form>

