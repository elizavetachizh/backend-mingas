<h2 class="page-title">edit product</h2>
<a href="/admin/admin_article" class="btn btn-primary">Назад ко всем статьям</a>
<br><br>

<form method="post" action="/admin/admin_article/edit-article/<%= id %>" enctype="multipart/form-data">

    <div class="form-group">
        <label for="">content</label>
       <textarea type="text" class="form-control" name="content" placeholder="content"><%= content %></textarea>
    </div>

     <div class="form-group">
               <label for="">Измените ссылку на картинку </label>
              <input type="text" class="form-control" name="image" id="img" value="<%= image %>">
          </div>

            <div class="form-group">
                  <label for="">button</label>
                  <input type="text" class="form-control" name="button" value="<%= button %>" placeholder="button">
              </div>
                <div class="form-group">
                      <label for="">link</label>
                      <input type="text" class="form-control" name="link" value="<%= link %>" placeholder="link">
                  </div>


    <div class="form-group">
        <label for="">href</label>
        <select name="href" class="form-control">
            <% href.forEach(function(el){ %>
                <option value="<%= el._id %>"
                <% if (el.name == el) { %>
                selected="selected"
                <% } %>
                ><%= el.name %></option>
            <% }); %>
        </select>
    </div>

    <button class="btn btn-default">Сохранить</button>
</form>

